This checked-in fixture home intentionally contains no agent configuration.
It is used by the scan CLI smoke test to verify --home stays inside the fixture
instead of reading the developer's real home directory.
